import { useState, useRef } from 'react';

function MyAccount() {
    const scrollContainer = useRef(null);

    return (
        <div className="mx-auto max-w-[760px] mt-3 md:mt-6">
            <p>boilerplate</p>
        </div>
    );
}

export default MyAccount;